/target
**/*.rs.bk
*.swp
*.swo
.DS_Store
*.apk
*.so
bot.sh
mkstubs.sh
stub_gen.sh

# Claude Code worktrees
.claude/worktrees/
*.core

# Per-crate cargo targets (tests build fixtures into crates/*/target)
crates/*/target/

__pycache__/
*.pyc

# Run-capture logs belong in runs/, not in git (project discipline: keep the tree clean)
/runs/*.txt
runs/cycle2/

# oversized binary capture (kept on disk, not in git)
/runs/*.rgb
