# Open-Sober STATUS.md (worker ledger)
## SH120 (Sep 14, 2026, hermes-worker): app-bridge dispatcher-node data seed — the SendAppEventOnAppReady [*x1-24]=[0-24] SIGSEGV is CLEARED. Opt-in ladder reaches setTaskSchedulerBM (trap = next gate). Workspace green; default unchanged (run-variable SH55/64 flake, NOT a regression).
- SH120: after SH119 the app-bridge event dispatch crashed at guest 0x102b561b0 (`ldur x8,[x8,#-24]`) — a GENERIC ~40-site leaf (do NOT code-patch) reading the shared dispatcher-node .bss global 0x10683a460 whose +0 self-link = 0 -> [0-24] fault. Seeded the DATA: node[+0]=0x10683a000 (0-clean self-link, base==node), node[+0x20]=1 (base[+32] active -> leaf returns 0 -> caller skips dispatch work) = benign "nothing registered" empty singleton. +0 write verified writable .bss. Opt-in.
- VERIFIED (JIT_SH115_SINGLETON_PATCH=1): the [*x1-24] SIGSEGV is GONE; ladder runs nativeGameGlobalInit Ok -> nativeUpdateAdapterInit Ok, then the process TRAPS (SIGTRAP/133) during the setTaskSchedulerBM rung — the app-bridge path now executes real engine code (next gate is the trap inside setTaskSchedulerBM, lr/guestpc in runs/sh120-run.txt).
- DEFAULT LADDER: run-variable (2/5 runs crash at setTaskSchedulerBM guestpc 0x106240c40 = the KNOWN SH55/64 concurrent-thread flake; 3/5 clean EXIT 0). NOT a regression: SH119/SH120 are flag-gated and log 0 on the default path (verified). Pre-existing, documented in lineage (SH55/64 run-variable 134/139).
- All 8 sh115-120 tests + sh111 baseline pass; workspace green.
- NEXT: (a) clear the setTaskSchedulerBM trap (the SH120 opt-in path) — likely the SH55/64 thread-desync or another unseeded engine object; (b) the real session lever (unchanged): StartLuaAppDM building the app-data-model -> getFilesDir fires -> rbx-storage.db -> GuiObjects, and MH_APP_READY/[0x106829ea8] type-4 wiring.
## SH119 (Sep 14, 2026, hermes-worker): SendAppEventOnAppReady's two UNGATED singleton lambdas patched — its body now advances into the app-bridge event-dispatch path (next gate: guestpc 0x102b561b0 self-link). Workspace green.
- SH119: SendAppEventOnAppReady (guest 0x102bb463c) STILL soft-returned through two UNGATED singleton lambdas the SH115 3-site patch + gate-force can't reach (they're on the gate-cleaned path's own registered callbacks, dispatching direct off the 0x60 vtable): lambda 0x6251610 (`ldr x8,[x8,#0xf0]`@0x6251670) + lambda 0x6260a68 (`ldr x8,[x8,#0x550]`@0x6260abc). Materialized the stable object into x0 at both (movz+movk rd=0; 5-slot window w/ nop + 3-slot window) + repointed the early-return branches (b.eq + cbz x19) to the epilogue. +1 sh119 test.
- VERIFIED: both lambdas patched, SendAppEventOnAppReady's body now EXECUTES past them into the app-bridge event-dispatch region (crash moved forward to guestpc 0x102b561b0). nativeGameGlobalInit still returns Ok; full ladder runs; render pipeline Ok.
- NEXT GATE: guestpc 0x102b561b0 (file 0x2b561b0, a BTI-boundary fn) faults on `ldur x8,[x8,#-24]` (the object passed in x1 has +0 self-link-offset = 0 -> [0-24] fault at 0xffff...ffe8, lr 0x102bc48e0) — an intrusive self-link field in the app-bridge event object that needs seeding. After it: getFilesDir fires -> rbx-storage.db -> app-data-model -> GuiObjects (the session-advance objective).
- All 8 sh115/116/117/118/119 tests + sh111 baseline pass; workspace green; default ladder (no flag) unregressed (EXIT 0).
## SH118 (Sep 14, 2026, hermes-worker): canary/render-ctx GOT dual-use fixed — the FULL ladder now runs CLEAN end-to-end (opt-in). nativeGameGlobalInit returns. Workspace green.
- SH118: the surface-handoff (V2UpdateSurface) rung aborted with `*** stack smashing detected ***`. Root cause (subagent): guest 0x1067d16f0 is BOTH the __stack_chk_guard GOT slot AND the engine's render-ctx singleton (SH14/SH106 dual-use); renderthunk re-publishes it to the live ctx, so a canary fn whose body re-publishes the slot mid-execution (V2UpdateSurface does via ANativeWindow/app-bridge) reads a mutated canary at epilogue vs prologue -> false __stack_chk_fail. NOT a stack collision (ladder is the only live boot_sp user; render idle). Fix: seed both guard slots (0x1067d16f0 + 0x10631aa30) to a stable leaked pointer (0x2f_2a_1a_0a_0e_0f_10_11) right before the surface rung — the canary fn caches the pointer at prologue, epilogue derefs the cached pointer, so *guard stays constant regardless of mid-body re-publication.
- **FULL LADDER CLEAN (opt-in):** with JIT_SH115_SINGLETON_PATCH=1 + SH115-118, the run is now EXIT 0 / 0 crashes / 0 stack-smash: StartApp Ok -> nativeInitializeNativeFlags (benign soft-return) -> nativeGameGlobalInit returned Ok (standing SH-wall since SH54!) -> nativeUpdateAdapterInit Ok -> setTaskSchedulerBM -> V2InitWithParams -> StartLuaAppDM returned Ok -> V2StartApp -> V1 AppStart -> V2UpdateSurface (soft) -> surface-handoff wired_xid=0x200000 [0x10683d348]=0x200000 (real XID in the engine's window slot) -> SendAppEventOnAppReady (soft). Render pipeline runs (renderthunk real ctx, taskv4-frame present swap Ok(0x1), renderframe Ok). Log: runs/sh118-run.txt.
- OPEN (session-advance wall): MH_FLAGS_LOADED=false MH_APP_READY=false; [0x106829ea8]=0 (type-4 vector not installed). StartLuaAppDM returns Ok but its session does not yet build the app-data-model / construct GuiObjects (the NativeHelper MH_* milestones are set-only, nothing polls them; SendAppEventOnAppReady body soft-returns at the singleton vtable — the SH114-identified next step: make V2Start/StartLuaAppDM actually execute their bodies).
- All 6 sh115/116/117/118 tests + sh111 baseline pass; workspace green; default ladder (no flag) unregressed (EXIT 0, StartLuaAppDM Ok, task frame present).
- NEXT: (a) make the SendAppEventOnAppReady/V2Init/V2Start bodies execute (not soft-return) so the session advances (MH_* milestones -> GuiObjects -> scene nodes -> engine self-constructs login/home); (b) then flip SH115-118 default-ON once the clean full-ladder is confirmed stable.
- SH117: after SH116, nativeInit's flag-registration loop called a hash-map probe helper (guest 0x10232090c / file 0x232090c, sole caller `bl 232090c`@0x23208d8) whose flag-map object has an UNSEEDED +0 bucket-array (stale host-stack garbage) -> `ldr x8,[x0]; ldr x8,[x8,x11,lsl#3]` SIGSEGV (fault==[table+0]). The map is a per-run leak (fixed-address seed unreliable), so leaf-rewrite the helper's 8B prologue to `ret`+`nop` (returns x0 = the non-zero map object) -> caller `cbz x0` takes "found"/insert-skip. Deterministic, idempotent, non-vtable-widening. +1 sh117 test.
- **BREAKTHROUGH (opt-in JIT_SH115_SINGLETON_PATCH=1):** the FULL rung ladder now runs on ONE jit_run: StartApp Ok -> nativeInitializeNativeFlags (benign soft-return, no crash) -> **nativeGameGlobalInit returned Ok** (Standing SH-wall gate — previously parked / never returned since SH54!) -> nativeUpdateAdapterInit Ok(0x1) -> setTaskSchedulerBM (soft) -> V2InitWithParams (soft) -> **StartLuaAppDM returned Ok** -> V2StartApp (soft) -> V1 AppStart (soft) -> V2UpdateSurface (soft), with the render pipeline running (renderthunk real ctx, taskv4-frame present #0 swap Ok(0x1), renderframe Ok). Remaining: one host-side SIGABRT during/after the V2UpdateSurface surface-handoff rung (next gate). Runs/sh117-run.txt.
- All 5 sh115/116/117 tests pass; workspace green; default ladder (no flag) unregressed.
- NEXT: (a) clear the surface-handoff SIGABRT (during --v2boot-surface-handoff) so SH115-117 can flip default-ON; (b) once the surface rung clears, drive StartLuaAppDM's app-data-model build (getFilesDir fires -> rbx-storage.db -> remembered session).
## SH116 (Sep 14, 2026, hermes-worker): singleton-dispatch patch (SH115) + nativeInit lock-owner helper patch; opt-in until the deeper nativeInit gate clears.
- SH115: scoped-patch the three nullable-singleton dispatch accessors (V2Init A 0x62517c0 / V2Start A2 0x6251aa4 / V1AppStart B 0x6260944) that 'soft-return' because their `blr x8` reads past the 0x60 vtable. Patch each site's 28B window to MATERIALIZE the stable zeroed 0x80 object (routeb_singleton_obj_addr, [OBJ+0]=benign 0x60 identity-leaf vtable) into x0, store it into objA[0], and return it — the differential remedy SH110/111 mandate (vtable widening dead-ended; blr->xzr -> NULL+0x28). Fixed an OWN re-entrancy bug: the patched store-slot is the shared `mov x0,xzr` tail that the early-return branches (b.eq + cbz x19) jump to BEFORE slot5 loads x9 — repointed all 6 such branches (2/site) to the epilogue. Encodings verified vs aarch64 assembler. 3 new sh115 tests (materialize window, sites, branch repoint).
- SH116: with the patch ON, nativeInit (rung 0) progresses past its former soft-return and hits `lock(&unmapped .bss-global +0x28)` — the global's page (guest 0x1072739c0, file 0x72739c0) is UNMAPPED at runtime (mprotect RW -> ENOMEM). Since it can't be seeded by a store, patched the helper (file 0x2320710/0x2320714/0x2320718 = guest 0x102320710) to materialize a stable all-zero 0x60 object into x0 (movz+movk hw1+hw2) so `add x0,x0,#0x28; bl pthread_mutex_lock` locks a valid PTHREAD_MUTEX_INITIALIZER. VERIFIED: the +0x28 getter gate CLEARS (crash moves deeper to guestpc 0x10232090c / a stack deref — next gate). +1 sh116 test.
- Empirically: with JIT_SH115_SINGLETON_PATCH=1 the render pipeline RUNS (renderthunk recovers real ctx, taskv4 present #0 swap Ok(0x1), renderframe swap Ok(0x1)) AND nativeInit advances past the lock — but the ladder still hits the next nativeInit gate (guestpc 0x10232090c, stack deref), so the patch stays OPT-IN.
- DEFAULT LADDER UNREGRESSED: all new code is inert without JIT_SH115_SINGLETON_PATCH=1 (routeb_singleton_obj_addr count=0 on default path); default --v2boot ladder re-verified EXIT 0 / 0 crash / StartLuaAppDM returned Ok / task-frame present swap Ok(0x1) / renderframe Ok across multiple runs (the earlier `sh115-default.txt` EXIT 134 was the KNOWN run-variable SH55/64 concurrency flake — 2 immediate re-runs clean).
- Workspace green (exit 0), arm64jit example tests 27/0 (sh111 baseline + sh115 x3 + sh116 + others).
- NEXT: (a) clear the current nativeInit guestpc 0x10232090c stack-deref gate so SH115 can flip default-ON (making the render pipeline part of the standard ladder); (b) after nativeInit completes, chase the app-data-model build so getFilesDir fires -> rbx-storage.db in the store -> remembered session.