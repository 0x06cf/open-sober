# Open-Sober STATUS.md (worker ledger)
## Session (Sep 17 2026, hermes-worker): SH243 — the NativeDataModelManager GETTER CELL was seeded at the WRONG ADDRESS for 8 cycles (SH165-240). Getter 0x102174c04 `adrp x8,7275000(+0x550); ldar x0,[x8]` reads GUEST 0x107275550 (RW data seg) — NOT 0x102727550 (which is vaddr 0x2727550 inside the R-E CODE seg). A/B on the real binary (6/6 completing, EXIT 124): seeding the TRUE cell -> StartLuaAppDM returns Ok(M)=fabricated manager (vs baseline Ok(0x3e8)) — session carries M. Guard now seeds BOTH cells (env-gated, idempotent). +hermetic sh243 (lib 391/0), examples 85/0, recon-v3 plane re-verified (24 frames swap Ok(0x1) no json abort). Doc docs/frontier-sh243-manager-cell-wrong-address.md, repro runs/capture_sh243_manager_cell.sh. Commit 6fb645f.
## Session (Sep 17 2026, hermes-worker): SH240 — DMCONT continuation RE-MEASURED FRESH at HEAD = block-entry-definitive negative (SH228 holds at newest state) + full dispatch chain byte-pinned (+1 hermetic, examples 85/0). Doc docs/frontier-sh240-dmcont-fresh-negative-pinned.md, repro runs/capture_sh240b_dmcont_flow.sh.
## Session (Sep 16, 2026, hermes-worker): SH232 — EC-world CALLER MECHANISM measured at runtime (in-rung EC callers never translate on the completing ladder; closes SH231a's static inference with a runtime mechanism). +1 hermetic (sh232, elfjit example 78/0). Doc docs/frontier-sh232-ec-caller-mechanism.md, repro runs/capture_sh232_ec_caller_mechanism.sh. Workspace green (389 arm64jit + all crates).
- **THE GAP (genuinely new):** SH231 measured the EC TARGET (ExperienceController DM-creation lambda world bodies [0x102e1c650,0x102e25200)) at 0 hits headlessly; SH231a's static caller-scan found 241 callers incl. two inside LADDER rungs (StartLuaAppDM 0x1023efe2c -> bl 0x2e24598 @0x1023f1294; V2InitWithParams deep-branch 0x23cfafc -> bl 0x2e24468 @0x1023cfd68). Open: are the caller bodies entered-as-blocks and diverted in-body, or do the rungs benign-complete BEFORE the caller block is translated?
- **MEASURED (runtime, completing-only):** 5 watched regions on the canonical --v2boot ladder; on BOTH govtail-positive (control-fired) runs (EXIT 124, governor tail walked to 0x102ea30dc) — EC TARGET + ALL FOUR caller/body blocks (StartLuaAppDM EC-arg 0x1023f12d0..0x1023f1300, V2Init EC-caller 0x1023cfd40..0x1023cfd70, V2Init mid-body 0x1023cfb40..0x1023cfc7c) = **0 hits**. Ladder sequence: StartLuaAppDM returned Ok(0x55a01d785a20) (benign soft-return) -> SH155 once-slot=0x400000b (intern, NOT a DM) -> V2StartAppWithParams -> outside-image stop. The rungs benign-complete UPSTREAM of their EC-call blocks -> the `bl`s are never translated on the ladder.
- **HONEST (do-not-over-claim):** closes SH231a's residual with a block-entry-definitive runtime mechanism; does NOT manufacture a DM, does NOT lift the Route-B live-DM structural gate (SH209/218/223/224/228/231 unchanged). Ladder ~1/3 flaky (SH198/SH55 V2Init singleton-vtable stop); conclusion on govtail-positive runs only (SH209/223/231 pattern).
- **CODE (+1 hermetic sh232, real-image guard family as sh231, skip-if-absent):** byte-pins EC-call `bl` words 0x1023f1294=0x9428ccc1->0x2e24598 + 0x1023cfd68=0x942951c0->0x2e24468, enclosing prologues (0x1023efe2c=0xd10183ff, 0x1023f11f4=0xa9bf7bfd, 0x23cfafc=0xa9bc7bfd), bl imm26 sign-extended target cross-check + window/4-alignment. No production path edited.
- **NEXT (honest):** Route-B live-DM = structural gate UNCHANGED. EC world located (SH231) + in-rung callers pinned + non-execution mechanism measured (SH232). Standing forward hook unchanged: SH174 capture-latch arming *(0x106391908) at a real make_shared. recon-v3 deliverables stay shipped + verified.
- **RE-VERIFIED at HEAD (ed9f076):** recon-v3 render plane = 24 task-driven frames (present #19..#23 swap Ok(0x1), distinct colors), 196 node pops, no json abort, 0 SIGSEGV/SIGABRT, EXIT 124. Hermetic sh232 + full workspace green (elfjit examples 78/0 --test-threads=1; the parallel bulk-run failures of sh225/226/228/231/232 are the pre-existing concurrent-ELF-load artifact, all pass serial). Sh232 edit was a pure test addition — no production path touched.
## Session (Sep 16, 2026, hermes-worker): SH230 — byte-pin the type-4 dispatch site (the recon-v3 self-driven-frame load-bearing opcodes) + ENTIRE SH210 latent wiring re-verified ARMED at HEAD (3/3 clean). Doc docs/frontier-sh230-type4-dispatch-bytepins.md. Workspace green (389 arm64jit + 76 elfjit examples + all crates).
- **THE GAP (genuinely open, not a re-tread):** recon-v3 deliverable SELF-DRIVEN FRAMES rides the type-4 dispatch site `adrp x8,6829000; ldr x3,[x8,#3752]; cbz x3,skip; ...; br x3` — the exact stream that br's into the seeded `0x106829ea8` vector to present a task-driven frame. SH211 byte-pinned the heartbeats (0x2856f24/0x2856f68) + wiring cells + SendAppEvent discriminator, but NOT this dispatch site's own opcodes. Extended the sh211 hermetic with 4 real-image byte-pins (verified LE on real libroblox.so 109,193,800 B): 0x2853784=0xd001fea8 adrp x8,6829000 / 0x2853788=0xf9475503 ldr x3,[x8,#3752] (the vector) / 0x285378c=0xb4001b23 cbz x3 null-vector skip / 0x28537b8=0xd61f0060 br x3 tail-dispatch.
- **SH210 FULL LATENT WIRING RE-VERIFIED at this exact HEAD (3/3 clean EXIT 124):** arm=3 xid=3 appevent=3 filesdir=3 dmprobe=3 — capture latch + G1 surface XID + G2 SendAppEvent 'Home' + G3 files-dir + SH155 DM-root probe ALL ARMED. recon-v3 plane green (24 task-driven frames, present #19..#23 swap Ok(0x1), 195 pops, no json abort, 0 crash).
- **HONEST:** pure regression hardening; does NOT manufacture a DM or move the live-DM wall. Route-B live-DM structural gate UNCHANGED; single forward hook stays SH174 capture-latch arming at a real make_shared.
- **UNREGRESSED:** cargo test --workspace EXIT 0 (389 arm64jit + all crates); examples 76/0; sh211 known-green.
- **WHY (a stale Route-B verdict worth re-measuring):** SH203 declared the post-family fault (NULL-singleton pthread_mutex_lock, host-call slot 0x7f00000022b0, lr=0x102b53a78, x0=0x28) a deterministic 4/12 live-world-build gate via "enclosing fragment 0x2b53a64 has 0 direct bl callers". SH205 later PROVED that exact method unreliable (miscounted; dismissed a seedable flag-manager fault as migration). The site was never GSDSP-analyzed, and SH116b + SH217 landed after SH203.
- **MEASURED — 0/28 post-family reproductions at HEAD:** two canonical V2_ONDEMAND batches (16 GSDSP + 12 exact SH203-env) = 25 clean EXIT 124 / 3 faults at ONLY known non-seedable classes (0x10284f490 SH55/64 `__call_once` blr-x2, 0x106240c24 FMOD crash-A, 0x1021dea94 SH208 singleton-vtable host-ptr) — NEVER the post-family site (SH203: 4/12 pre-SH116b/SH217). SH203's site and SH116b's site are both NULL+0x28-mutex singletons (same family) ⇒ closure consistent with SH116b absorbing it or SH217 shifting the dispatch. The "post-family = structural gate" verdict is no longer measurably alive at HEAD.
- **HONEST (do-not-over-claim):** 28-run non-reproduction = strong evidence, not proof of impossibility. It does NOT manufacture a DataModel — Route-B live-DM structural gate UNCHANGED (do-init once-lambda still yields intern 0x400000b, SH155 liveDM=false). Residuals all documented non-seedable, no seed warranted. No production code change.
- **CODE (+1 hermetic sh219, real-image guard family):** byte-pins SH116b flag-manager slot (0x2320a24=0xf0027a88 adrp x8,7273000 / 0x2320a2c=0xaa0303e4 / 0x2320a30=0xf944d908 ldr[0x10672739b0]) + SH203 post-family fragment (0x2b53a64=0x940140e4 bl receiveCall+0x558 / 0x2b53a6c=0xa9bf7bfd / 0x2b53a74=0x94de0a4f bl pthread_mutex_lock@plt) + guest transform + 4-alignment for 6 .text sites. Verified against real libroblox.so.
- **UNREGRESSED at HEAD:** recon-v3 (24 task-driven frames present #19..#23 swap Ok(0x1), 196 pops, no json abort, 0 crash) + SH174 capture-latch ARMED+DELEGATING (ACTIVE 0x1067daaf0 → engine's own 0x1021ebaf4).
- **STANDING (do-not-re-tread):** Route-B live-DM = structural gate (SH196/203/204/209/218). This cycle removes a possibly-FALSE "live-world-build gate" from the closure set. SH174 capture-latch arming *(0x106391908) at a real make_shared = the single forward hook. recon-v3 deliverables shipped + verified.
## Session (Sep 16, 2026, hermes-worker): SH212 — RESIDUAL LADDER FLAKES CHARACTERIZED at HEAD (FMOD/AAudio audio-subsystem CONTACT + raced flag-manager system_error, both non-seedable) + recon-v3 + full-ladder completion re-verified. Doc docs/frontier-sh212-residual-flakes-characterized.md, repro runs/capture_sh212_residual_flakes.sh. Workspace green (cargo test --workspace EXIT 0; arm64jit lib 387/0 + all crates).
- **MEASURED (8 canonical ladder runs, SH210 env + GSDSP + outside-trace): 7/8 CLEAN EXIT 124** — full V2 ladder (nativeInitFlags → gameGlobalInit → do-init once self-latches → governor MODERN router [0x106a70880]=1 → V2Confirm → V2Start → V1 AppStart), 0 crash. Residuals: run-2 SIGSEGV guest 0x106240d8c (fault=0x0, x0=0); run-8 libc++abi `std::__ndk1::system_error` at nativeInitializeNativeFlags.
- **Crash A = AUDIO-pillar first contact (forward evidence):** guest 0x106240d8c = file 0x6240d8c inside `Java_org_fmod_FMOD_OutputAAudioHeadphonesChanged` — FMOD audio init now reaches its AAudio output/headphone-format path headlessly on a no-audio box; NULL-`this` in its strstr/strtol format parse faults run-variably. GSDSP fired, all host-heap + sign-extended HOST ptr, BT->HOST dispatcher, NO in-image GUEST caller ⇒ host-allocated unmaterialized audio device — NON-SEEDABLE (SH55/64+SH206 class). Future audio-hardening anchors to guest 0x106240d8c.
- **Crash B = raced resource throw (1/8):** nativeInitializeNativeFlags after "Registered Flag Provider ID from Java"; `.what()` garbage stack words = unconstructed-guest-stack-string family. Non-seedable. SH116/SH116b lock fix re-verified firing every run (0x102320a98 gone); residual ≠ SH116b.
- **STANDING UNCHANGED:** Route-B live-DM = structural gate (SH209 do-not-re-tread). recon-v3 green (24 task-driven frames, 195 pops, no json abort) + SH210 wiring ARMED. No prod change (both flakes non-seedable).
## Session (Sep 16, 2026, hermes-worker): SH208 — LADDER FLAKE CHARACTERIZATION: residual run-variable dispatch flakes are NOT the SH202 on-demand patcher (exonerated, measured A/B); recon-v3 immediate-priority deliverables re-verified green at HEAD. Doc docs/frontier-sh208-flakechar.md. Workspace green (cargo test --workspace EXIT 0, 0 failures). No production change (do-not-chase, no seed).
## Session (Sep 16, 2026, hermes-worker): SH207 — RESOLVER-MAP GATE CLOSED AT FRESH SOURCE-DEPTH + futex-requeue flake FIXED. Workspace green. Commit 9f39594.
- **FRESH MEASUREMENT (2/2 clean ladder runs):** the bulk-registrar **SOURCE map 0x106dca0e90 is EMPTY {0,0}** — the one container SH191/194 never read (dest 0xe70 + register 0xf60 only). Region-watch confirms registrar 0x2208ae8 exercised in-ladder (0x102208ae8/b6c/cf8/e38 = rehash+insert blocks) but with source+dest both EMPTY it cleanly **no-ops**, NOT corruption-destined. Reconciles SH191/194 "resolver EMPTY" with SH194 "registrar region fires every run"; SH192/194's bad_weak_ptr = driving registrar standalone w/ fabricated key (not repeated, measured only).
- **VERDICT (do-not-re-tread):** getService name→classid resolution sits behind the live class-registry world-build that populates the source — Route-B live-DM structural gate UNCHANGED. No headless seed populates the resolver source.
- **GENUINE PROD FIX:** `futex_requeue_actually_moves_waiter` flake fixed (waiter WAIT timeout 5s→60s; REQUEUE side spins ~20s; short timeout made the flake under parallel load). Verified green under full workspace load.
- **+1 hermetic** sh207 (three registry-map addresses/adjacency/source-selector 0x20-past-dest).
## Session (Sep 16, 2026, hermes-worker): SH202 — ON-DEMAND single-site V2 singleton-dispatch family patcher (SH201 §6's named lever). Commit 783cd28. Doc docs/frontier-sh202-v2ondemand.md. FIRST full V2 ladder completion at HEAD. Workspace green (564/0, +3; arm64jit lib 386/0, elfjit example 64/0).
- **CODE (arm64jit/src/jit.rs, default-inert, env JIT_ROUTEB_V2_ONDEMAND):** `v2_family_bl_target_l` (imm26 branch decode, CORRECT sign-ext in i64: subtract 2^26=0x400_0000 NOT 2^30 — the SH201-style bug, falsified by a backward-bl regression test), `v2_family_blr_from_guest` (pure family classifier: bl 0x6249eb8 getter within 16 back + ldr x8,[x0] after + past-0x60 ldr x8,[x8,#N] within 4 of blr), `v2_ondemand_object` (stable leaked 0x80 all-leaf singleton), `v2_ondemand_patch_at` (classify+patch+drop cache+return rewind pc). Hooked into the outside-image stop in jit_run_inner; rewinds state.pc to the window start and continues. Clears ONLY the exact blr the run dispatches through (avoids SH201's family-wide over-patch SIGABRT). +4 hermetic incl. real-image guard (3 stop sites + 4 SH200 sites ALL classify).
- **EMPIRICAL (real libroblox.so, llvmpipe, canonical 9-rung ladder):** with V2_ONDEMAND=1 the run patches exactly the site(s) it dispatches through (1-3/run) and returns past them. **FULL LADDER COMPLETION OBSERVED**: V2InitWithParams Ok(0x3e8) → V2StartAppWithParams Ok(0x3e8) → V1 AppStart__ Ok → V2UpdateSurface Ok (XID 0x200000) → SendAppEventOnAppReady Ok(0x3e8) → EXIT 124, 0 crash (runs/sh202-ondemand-full-ladder.txt). FIRST full V2-ladder completion (V2Start was "observed, run-variable" at SH200). **Honest boundary:** 1/4 runs completes clean; 3/4 patch the family then advance DEEPER into a NEW reachable wall — SIGSEGV pc=0x7f00000022b0 (host-call slot) fault [x0+0x28] lr=0x102b53a78 — a host thunk deref'ing a NULL/small arg, the next seed target (forward motion, not regression). **Default path unchanged** (V2_ONDEMAND off ⇒ 0 patches, EXIT 124, SH200/201 baseline).
- **STANDING:** Route-B live-DM world-build = structural gate (unchanged). The new post-family fault (host thunk slot 0x22b0, fault [x0+0x28], lr 0x102b53a78) is the next unblocked seed: drive whatever object lr's caller threads into it, or confirm it's the same live-world-build gate (do-not-chase if so). Keep do-init→app-shell→governor continuation + live-DM line as the primary front.
## Session (Sep 15, 2026, hermes-worker): SH178 — Route-B cone CORRECTION: the "ExperienceController::join make_shared<DataModel>" premise is MISATTRIBUTED (reloc-level law wall). Workspace green (550/0). Doc docs/frontier-sh178-routeb-join-misattribution.md. No code change.
- **3-agent fresh Route-B cone (operator's "return to Route B" directive) + independent packed-RELA decode:**
  (1) The text the prior cones cited for "the only make_shared<DataModel> is inlined at
  ExperienceController::join" is MISLABELLED. file 0x2206c40/0x2206d74 = GlobalInit do-init
  (ldar once-guard -> bl 0x2173b3c -> store [0x106a68408]; operator-news only 0x28/0x40/0x100/0x1000,
  no DM-size). file 0x2173b3c = string-intern GetOrCreate (strcmp + hash-bucket), NOT an RTApp
  app-registry / DM allocator. 0x2206db8 dispatch needs [obj+#32]=real vtable'd obj whose +0x30=
  real ctor = the SH156 JIT_ROUTEB_DM_SEED path to the governor dead-head. The genuine DM alloc
  site was NEVER located — it sits in a region no cone reached.
  (2) LAW-LEVEL WALL PROVEN AT THE RELOC LEVEL (main-loop decode of 568,272 packed ANDROID_RELA):
  the reloc writing guest 0x106391908 (setDataModelToCurrent getter return / SH172 holder) =
  (0x6391908, R_AARCH64_RELATIVE, addend 0x6358d40) -> loader writes base+0x6358d40 from an
  all-zero on-disk .data.rel.ro cell; 490 relocs populate [0x1063915a0,0x106392600). The DM
  holder/registry is RELOCATION- + SESSION-populated (wired consumer std::function + live current
  shared_ptr exist only post-session) — circular with the construction we lack. Not seedable.
  (3) R1 (synthetic CoreScript) reconfirmed dead for the CORRECT reason: rbxasset://scripts/
  CoreScripts at 3 pull-only URI-builder sites; CoreScriptLoader 0x101f1d8ac has ZERO direct bl
  callers; filesdir 0x10726d600 never joins a CoreScripts Lua path. R2 (UniversalApp.rbxm) still
  blocked on the same live-DM (0x229006/0x288090/0x2d87b18). Type-4 producer [0x106829ea8]
  latent-only. Present-walker node-list = Route-A plane (do-not-polish).
- **VERDICT:** do NOT build the headless dynamic-DM-trace attempt (re-treads SH156/164c, targets a
  misattributed address, expected fault on .data.rel.ro registry). Live-DM = LAW-level migration
  wall; the SH169 DELEGATE+VALIDATE capture latch is the ready observer for a real app-launch
  (GPU host / real input). Future re-chase must first locate the genuine join inline via a fresh
  packed-RELA decode of the DM vtable/ctor, not reuse 0x2206d74/0x2173b3c.
- Cookie line fully persisted (SH175/176/177 committed, 3006fd3/8ca920e/060eeb7). Workspace
  550/0. /tmp cleaned (453M used on 7.7G).